# TermDM Configuration File
# This file should be installed at /etc/termdm/config

# Default username to authenticate
username=samsterjam

# Session command to execute after successful login
# Examples:
#   startx              - Start X11 with ~/.xinitrc (auto-adds -- vt1)
#   sway                - Start Sway (Wayland)
#   Hyprland            - Start Hyprland
#   startx /usr/bin/i3  - Start specific WM
#
# Note: For startx, termdm automatically adds "-- vt1" to use the current VT
session=startx
